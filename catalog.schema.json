{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "catalog.schema.json",
  "title": "pc-setup Catalog",
  "description": "Schema for catalog.json — defines apps and scripts to run during PC setup.",
  "type": "object",
  "required": ["version", "defaults", "items"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "version": {
      "type": "string",
      "description": "Schema version of this catalog file.",
      "examples": ["1.0"]
    },
    "defaults": {
      "type": "object",
      "description": "Default values applied to all items unless overridden.",
      "required": ["priority"],
      "additionalProperties": false,
      "properties": {
        "priority": {
          "type": "integer",
          "minimum": 0,
          "description": "Fallback execution priority when an item does not specify one. Lower values run first."
        }
      }
    },
    "items": {
      "type": "array",
      "description": "Ordered list of catalog items (apps and scripts).",
      "minItems": 1,
      "items": { "$ref": "#/definitions/item" }
    }
  },
  "definitions": {
    "item": {
      "type": "object",
      "required": ["id", "type", "category", "displayName", "requiresReboot"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-zA-Z0-9]+\\.[a-zA-Z0-9]+$",
          "description": "Unique step identifier in 'category.name' format (e.g. 'dev.vscode')."
        },
        "type": {
          "type": "string",
          "enum": ["app", "script"],
          "description": "Item type — 'app' installs via winget; 'script' runs a PowerShell script."
        },
        "category": {
          "type": "string",
          "description": "Display category used for grouping in the Main Menu checklist."
        },
        "displayName": {
          "type": "string",
          "description": "Human-readable label shown in the checklist and reports."
        },
        "priority": {
          "type": "integer",
          "minimum": 0,
          "description": "Execution order — lower values run first. Inherits from defaults.priority when omitted."
        },
        "requiresReboot": {
          "type": "boolean",
          "description": "When true, the user is advised to reboot after this step completes."
        }
      },
      "allOf": [
        {
          "if": { "properties": { "type": { "const": "app" } }, "required": ["type"] },
          "then": {
            "required": ["winget"],
            "properties": {
              "winget": { "$ref": "#/definitions/wingetConfig" }
            }
          }
        },
        {
          "if": { "properties": { "type": { "const": "script" } }, "required": ["type"] },
          "then": {
            "required": ["script"],
            "properties": {
              "script": { "$ref": "#/definitions/scriptConfig" }
            }
          }
        }
      ]
    },
    "wingetConfig": {
      "type": "object",
      "description": "Winget install configuration for an app item.",
      "required": ["id", "source", "scope"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "description": "Winget package identifier (e.g. 'Microsoft.VisualStudioCode')."
        },
        "source": {
          "type": "string",
          "description": "Winget source name (e.g. 'winget')."
        },
        "scope": {
          "type": "string",
          "enum": ["machine", "user"],
          "description": "'machine' installs for all users; 'user' installs for the current user only."
        },
        "override": {
          "type": ["string", "null"],
          "description": "Optional raw argument string passed verbatim to winget via --override."
        }
      }
    },
    "scriptConfig": {
      "type": "object",
      "description": "PowerShell script configuration for a script item.",
      "required": ["path"],
      "additionalProperties": false,
      "properties": {
        "path": {
          "type": "string",
          "description": "Relative path to the PowerShell script file (e.g. 'scripts/explorer-show-extensions.ps1')."
        },
        "parameters": {
          "type": "object",
          "description": "Optional key/value parameters passed to the script as a hashtable.",
          "additionalProperties": true
        },
        "restartExplorer": {
          "type": "boolean",
          "description": "When true, Explorer is restarted after the script completes.",
          "default": false
        }
      }
    }
  }
}
